import {services} from '../content';
import {serviceDetails} from '../service-details';
import {absolute} from '../search-config';
import {SearchShell,JsonLd,organization,breadcrumbs} from '../search-structure';
export const metadata={title:'In-home care services | Astonishing Touch',description:'Explore nine types of everyday support, from companionship and respite to personal care, meals and errands. Contact our Creve Coeur office.',alternates:{canonical:absolute('/services')}};
export default function Services(){return <SearchShell><JsonLd data={{'@context':'https://schema.org','@graph':[organization,breadcrumbs([{name:'Home',path:'/'},{name:'Services',path:'/services'}])]}}/><p className="eyebrow">SUPPORT THAT FITS YOUR ROUTINE</p><h1>Our in-home care services</h1><p className="resource-lead">Explore everyday support and talk with our Creve Coeur team about the services that fit your needs. Confirm availability at your address directly with staff.</p><div className="resource-grid">{services.map(s=><article key={s.id}><h2><a href={'/services/'+s.id}>{s.title}</a></h2><p>{serviceDetails[s.id].definition}</p><a href={'/services/'+s.id}>Explore {s.title.toLowerCase()} →</a></article>)}</div><h2>Start with the routines that matter</h2><p>Tell the team where help would make a difference. Discuss your preferences, timing, service availability and prices with a live employee.</p><a className="button" href="/contact">Contact our care team</a></SearchShell>}
