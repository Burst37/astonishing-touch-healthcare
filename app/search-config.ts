// Keep private review blocked. Change only with an authorized public launch.
export const searchConfig={origin:'https://www.astonishingtouchhealthcare.org',publicIndexing:false};
export const absolute=(path:string)=>searchConfig.origin+path;
