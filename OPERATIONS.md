
## Ask Brittany voice and public discovery
The original logo was downloaded from the existing official Wix site. Avatar generation references the founder in the supplied team image. Voice is synthetic, not a voice clone. `/api/speech` supports Gemini Flash TTS via server secret `GEMINI_API_KEY`, optional `GEMINI_TTS_MODEL` (default gemini-3.1-flash-tts-preview), and `GEMINI_TTS_VOICE` (default Sulafat). No key is currently configured; the UI explicitly identifies browser speech fallback. The microphone uses browser SpeechRecognition where available, with denial/error text and typed fallback, and closes on panel dismissal. Speech requests are limited, no-store, and same-origin checked. No live AI provider is connected: CARE_AI_ENDPOINT, CARE_AI_KEY and CARE_AI_MODEL enable the existing bounded AI service. Test microphone permission, audio playback and actual Gemini quality on target devices after connection.

This review site remains private and noindex. Public search ranking requires an explicitly authorized public launch/custom-domain cutover, removing the noindex/robots disallow, and submitting the final canonical URLs to Google Search Console and Bing Webmaster Tools. Organization/Founder/WebSite/Service/FAQ structured data, descriptive visible service copy, social metadata and llms.txt prepare discovery; llms.txt has no guaranteed ranking effect. Do not claim rankings or add invented reviews, credentials or service areas.

Gallery assets: five reviewed OpenArt outputs plus three existing supplied branded photographs. New home-visit correction was excluded because its logo was visibly distorted; existing supplied doorway photograph retained. Generated gallery scenes are identified as illustrative, with activities to be confirmed against the care plan. Source images are optimized to 1800px WebP for delivery.

Voice correction: automatic Web Speech synthesis fallback removed after user rejected its robotic sound. Gemini TTS remains implemented but unconnected; failures return readable text and a clear unavailable message. No Gemini voice or live LLM has been represented as active. Browser speech recognition remains available for microphone input. Connect GEMINI_API_KEY to activate the existing gemini-3.1-flash-tts-preview / Sulafat synthetic voice and audition on the user's device. AI text generation still needs provider configuration separately.

## Gemini activation and grounded receptionist
Gemini API access verified and runtime secrets configured, environment revision 2. Answer model: gemini-2.5-flash through Google's compatible chat endpoint. Speech model: gemini-3.1-flash-tts-preview, voice Sulafat. Direct provider checks returned a correct business-hours answer and 24kHz mono PCM audio. No secret is stored in source.

`app/care-knowledge.ts` is the maintained knowledge/instruction source. Business/services/FAQs are imported directly from site content, so those edits automatically reach the assistant. Additional website behavior and curated general care concepts include official NIA and Medicare source URLs and review date. Up to eight prior messages provide short conversation context; request size, role and field limits reject malformed history. Conversation history is untrusted and cannot authorize actions or change business facts. This is runtime grounding, not model fine-tuning or a live web crawler. New company policies and additional care guidance should be reviewed before adding them. The assistant cannot book, send notifications, transfer calls or verify individual benefits.

Pricing rule: any explicit pricing/cost/rates/fees/quotes/discount/payment question bypasses AI and returns a live-employee referral with office phone and inquiry option. Related short follow-ups after a pricing question are also routed to staff. UI renders a direct click-to-call button. No price estimates, invented rates or claimed live transfers.

## ElevenLabs voice
Selected voice: Sharon Duvall — Warm, Confident, Engaging (hCLMtDrFPq4THEIO6zWf). Creator description identifies a warm African American female conversational voice; middle-aged catalog label does not certify an exact age. Direct speech generation succeeded with eleven_turbo_v2_5, MP3 44.1kHz / 128kbps, stability .5, similarity .75, style .15, speed .95. The key can read voices and synthesize this public voice, but cannot add voices to the account collection (voices_write absent); no collection mutation succeeded or was required for synthesis. SPEECH_PROVIDER=elevenlabs selects this provider; no silent voice substitution occurs on an error. GEMINI_API_KEY and CARE_AI settings remain for AI answers and optional explicitly configured Gemini speech. New ElevenLabs key stored as a runtime secret only.

Conversational upgrade: answer model Gemini 3.8 Flash (API verified), 20 recent turns, flexible answer depth with 2400 output-token safety budget. Removed blanket keyword bypass for health-adjacent care questions; clinical boundaries and deterministic pricing handoff remain. Latest user-selected ElevenLabs voice: QYnD9uIhJYIZWxWq8uxt. Speech is delivered in prefetched passages; microphone resumes after a reply in explicit conversation mode. End conversation cancels playback and prevents pending replies from starting audio. Browser microphone behavior still depends on browser support and permission.

Latest voice selection: P7x743VjyZEOihNNygQ9. ELEVENLABS_SPEED controls delivery pace; set to 1.08 after feedback that the previous voice was too slow. Validated range is clamped to 0.7–1.2, default 1.0.

Added live Google Search grounding for current/local/national news, with source links, checked date and sandboxed search suggestions. Defaults local scope to Creve Coeur / greater St. Louis. Unverified lookup failures are disclosed, not filled with stale headlines. Directions opens Google Maps at the verified office address. Friendly humor is supported. Optional BOOKING_URL (HTTPS) enables a scheduler handoff in chat; it is not configured, and no confirmed booking is claimed. Google Calendar discovery was blocked by insufficient connector scopes. Production booking requires the business scheduler or calendar integration and appointment rules.
